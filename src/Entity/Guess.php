<?php

declare(strict_types=1);

namespace App\Entity;

/**
 * @internal Stub — completed in Stage 6.
 * Exists so Rule::evaluate() can type-hint this class at compile time.
 * No ORM mapping → no schema impact.
 */
final class Guess
{
}
