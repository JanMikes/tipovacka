<div{{ attributes.defaults({class: 'bg-white rounded-2xl shadow-lg p-6'}) }}>
    <h2 class="text-lg font-semibold text-navy-900 mb-4">Žebříček</h2>

    {% if this.leaderboard.rows|length == 0 %}
        {% if is_granted('group_manage_members', this.group) %}
            <twig:EmptyState
                illustration="leaderboard"
                title="Tabulka je zatím prázdná"
                body="Jakmile první člen zadá tip, objeví se tady žebříček."
                ctaLabel="Pozvat kamarády"
                ctaPath="{{ path('portal_group_detail', {id: this.group.id.toRfc4122}) }}"
            />
        {% else %}
            <twig:EmptyState
                illustration="leaderboard"
                title="Tabulka je zatím prázdná"
                body="Jakmile první člen zadá tip, objeví se tady žebříček."
            />
        {% endif %}
    {% else %}
        <table class="w-full text-sm">
            <thead>
                <tr class="text-left text-gray-500 border-b border-gray-200">
                    <th class="py-2 w-12">Pořadí</th>
                    <th class="py-2">Hráč</th>
                    <th class="py-2 text-right">Body</th>
                </tr>
            </thead>
            <tbody>
                {% for row in this.leaderboard.rows %}
                    <tr class="border-b border-gray-100">
                        <td class="py-2 font-semibold text-navy-900">
                            {{ row.rank }}.{% if row.isTieResolvedOverride %} <span class="text-[10px] text-gray-500" title="Rozřazeno vlastníkem">(R)</span>{% endif %}
                        </td>
                        <td class="py-2">
                            <a
                                href="{{ path('portal_group_leaderboard_member', {groupId: this.group.id.toRfc4122, userId: row.userId.toRfc4122}) }}"
                                class="text-cyan-500 hover:underline"
                            >
                                {{ row.nickname }}
                                {% if row.fullName %}<br><small class="text-xs font-normal text-navy-900/60">{{ row.fullName }}</small>{% endif %}
                            </a>
                        </td>
                        <td class="py-2 text-right font-semibold">{{ row.totalPoints }}</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    {% endif %}
</div>
