{% extends 'admin/layout.html.twig' %}

{% block title %}Upravit turnaj — Admin — {{ brand_name }}{% endblock %}

{% block admin_breadcrumbs %}
    <li aria-hidden="true" class="text-white/30">/</li>
    <li><a href="{{ path('admin_tournament_list') }}" class="hover:text-accent-300 transition-colors">Turnaje</a></li>
    <li aria-hidden="true" class="text-white/30">/</li>
    <li class="text-white/80 font-medium" aria-current="page">Upravit</li>
{% endblock %}

{% block admin_title %}Upravit turnaj{% endblock %}
{% block admin_subtitle %}{{ tournament.name }}{% endblock %}
{% block admin_actions %}
    <a href="{{ path('admin_tournament_list') }}" class="btn btn-ghost btn-sm">
        <twig:ux:icon name="lucide:arrow-left" class="h-4 w-4" />
        Zpět na turnaje
    </a>
{% endblock %}

{% block admin_body %}
<div class="card-glass p-6 md:p-8 max-w-3xl">
    {{ form_start(form, {attr: {class: 'space-y-5', novalidate: 'novalidate'}}) }}
        <div class="field">
            {{ form_label(form.name) }}
            {{ form_widget(form.name) }}
            {{ form_errors(form.name) }}
        </div>
        <div class="field">
            {{ form_label(form.description) }}
            {{ form_widget(form.description) }}
            {{ form_errors(form.description) }}
        </div>
        <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
            <div class="field">
                {{ form_label(form.startAt) }}
                {{ form_widget(form.startAt) }}
                {{ form_errors(form.startAt) }}
            </div>
            <div class="field">
                {{ form_label(form.endAt) }}
                {{ form_widget(form.endAt) }}
                {{ form_errors(form.endAt) }}
            </div>
        </div>
        {% if form.creationPin is defined %}
            <div class="field">
                {{ form_label(form.creationPin) }}
                {{ form_widget(form.creationPin) }}
                {{ form_help(form.creationPin) }}
                {{ form_errors(form.creationPin) }}
            </div>
        {% endif %}
        <div class="flex flex-wrap gap-3 pt-2">
            <button type="submit" class="btn btn-primary">
                Uložit změny
            </button>
            <a href="{{ path('admin_tournament_list') }}" class="btn btn-ghost">
                Zrušit
            </a>
        </div>
    {{ form_end(form) }}
</div>
{% endblock %}
