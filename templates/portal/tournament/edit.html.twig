{% extends 'base.html.twig' %}

{% block title %}Upravit turnaj — {{ brand_name }}{% endblock %}

{% block body %}
<div class="mx-auto w-full max-w-2xl px-4 py-8 sm:px-6 sm:py-12 lg:px-8">
    <twig:Breadcrumbs :items="[
        {label: 'Nástěnka', path: path('portal_dashboard')},
        {label: tournament.name, path: path('portal_tournament_detail', {id: tournament.id.toRfc4122})},
        {label: 'Upravit turnaj'},
    ]" />

    <header class="mb-8 mt-2">
        <span class="eyebrow">Správa turnaje</span>
        <h1 class="mt-2 text-3xl font-black tracking-tight text-white sm:text-4xl">Upravit turnaj</h1>
        <p class="mt-2 text-sm leading-relaxed text-white/70">{{ tournament.name }}</p>
    </header>

    <div class="card-glass p-6 sm:p-8">
        {{ form_start(form, {attr: {class: 'space-y-5', novalidate: 'novalidate'}}) }}

            <div class="field">
                {{ form_label(form.name, null, {label_attr: {class: 'field-label'}}) }}
                {{ form_widget(form.name, {attr: {class: 'input'}}) }}
                {{ form_errors(form.name) }}
            </div>

            <div class="field">
                {{ form_label(form.description, null, {label_attr: {class: 'field-label'}}) }}
                {{ form_widget(form.description, {attr: {class: 'input'}}) }}
                {{ form_errors(form.description) }}
            </div>

            <div class="grid grid-cols-1 gap-4 sm:grid-cols-2">
                <div class="field">
                    {{ form_label(form.startAt, null, {label_attr: {class: 'field-label'}}) }}
                    {{ form_widget(form.startAt, {attr: {class: 'input'}}) }}
                    {{ form_errors(form.startAt) }}
                </div>

                <div class="field">
                    {{ form_label(form.endAt, null, {label_attr: {class: 'field-label'}}) }}
                    {{ form_widget(form.endAt, {attr: {class: 'input'}}) }}
                    {{ form_errors(form.endAt) }}
                </div>
            </div>

            {% if form.creationPin is defined %}
                <div class="field">
                    {{ form_label(form.creationPin, null, {label_attr: {class: 'field-label'}}) }}
                    {{ form_widget(form.creationPin, {attr: {class: 'input font-mono uppercase tracking-wider'}}) }}
                    {% if form.creationPin.vars.help %}
                        <p class="mt-1 text-xs text-white/50">{{ form.creationPin.vars.help }}</p>
                    {% endif %}
                    {{ form_errors(form.creationPin) }}
                </div>
            {% endif %}

            <div class="flex flex-wrap gap-3">
                <button type="submit" class="btn btn-primary">
                    <twig:ux:icon name="lucide:save" class="h-4 w-4" />
                    Uložit změny
                </button>

                <a href="{{ path('portal_tournament_detail', {id: tournament.id.toRfc4122}) }}" class="btn btn-ghost">
                    Zrušit
                </a>
            </div>

        {{ form_end(form) }}
    </div>
</div>
{% endblock %}
