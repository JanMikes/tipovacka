{% extends 'base.html.twig' %}

{% block title %}Upravit skupinu — Tipovačka{% endblock %}

{% block body %}
<div class="max-w-2xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
    <twig:Breadcrumbs :items="[
        {label: 'Nástěnka', path: path('portal_dashboard')},
        {label: group.name, path: path('portal_group_detail', {id: group.id.toRfc4122})},
        {label: 'Upravit skupinu'},
    ]" />
    <div class="mb-8">
        <h1 class="text-3xl font-bold text-navy-900">Upravit skupinu</h1>
        <p class="text-navy-900/60 mt-1">{{ group.name }}</p>
    </div>

    <div class="bg-white rounded-2xl shadow-card ring-1 ring-navy-900/5 p-8">
        {{ form_start(form, {attr: {class: 'space-y-5', novalidate: 'novalidate'}}) }}

            <div>
                {{ form_label(form.name, null, {label_attr: {class: 'block text-sm font-medium text-navy-900 mb-1.5'}}) }}
                {{ form_widget(form.name, {attr: {class: 'w-full px-4 py-2.5 border border-navy-100 rounded-lg focus:ring-2 focus:ring-cyan-500 focus:border-transparent outline-none transition'}}) }}
                {{ form_errors(form.name) }}
            </div>

            <div>
                {{ form_label(form.description, null, {label_attr: {class: 'block text-sm font-medium text-navy-900 mb-1.5'}}) }}
                {{ form_widget(form.description, {attr: {class: 'w-full px-4 py-2.5 border border-navy-100 rounded-lg focus:ring-2 focus:ring-cyan-500 focus:border-transparent outline-none transition'}}) }}
                {{ form_errors(form.description) }}
            </div>

            <div class="border-t border-navy-100 pt-5 space-y-5">
                <h2 class="text-sm font-semibold uppercase tracking-wider text-navy-900/50">Tipy a uzávěrka</h2>

                <div>
                    <label class="flex items-start gap-3 cursor-pointer">
                        {{ form_widget(form.hideOthersTipsBeforeDeadline, {attr: {class: 'mt-0.5 h-5 w-5 rounded border-navy-200 text-cyan-500 focus:ring-cyan-500'}}) }}
                        <span class="text-sm">
                            <span class="block font-medium text-navy-900">{{ form.hideOthersTipsBeforeDeadline.vars.label }}</span>
                            {% if form.hideOthersTipsBeforeDeadline.vars.help %}
                                <span class="block text-xs text-navy-900/60 mt-1">{{ form.hideOthersTipsBeforeDeadline.vars.help }}</span>
                            {% endif %}
                        </span>
                    </label>
                    {{ form_errors(form.hideOthersTipsBeforeDeadline) }}
                </div>

                <div>
                    {{ form_label(form.tipsDeadline, null, {label_attr: {class: 'block text-sm font-medium text-navy-900 mb-1.5'}}) }}
                    {{ form_widget(form.tipsDeadline, {attr: {class: 'w-full px-4 py-2.5 border border-navy-100 rounded-lg focus:ring-2 focus:ring-cyan-500 focus:border-transparent outline-none transition'}}) }}
                    {% if form.tipsDeadline.vars.help %}
                        <p class="text-xs text-navy-900/60 mt-1">{{ form.tipsDeadline.vars.help }}</p>
                    {% endif %}
                    {{ form_errors(form.tipsDeadline) }}
                </div>
            </div>

            <div class="flex gap-3">
                <button
                    type="submit"
                    class="bg-navy-900 hover:bg-navy-700 text-white py-3 px-6 rounded-lg font-medium transition-colors"
                >
                    Uložit změny
                </button>

                <a
                    href="{{ path('portal_group_detail', {id: group.id.toRfc4122}) }}"
                    class="bg-navy-50 hover:bg-navy-100 text-navy-900 py-3 px-6 rounded-lg font-medium transition-colors"
                >
                    Zrušit
                </a>
            </div>

        {{ form_end(form) }}
    </div>
</div>
{% endblock %}
